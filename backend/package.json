{
  "name": "csv-manager-backend",
  "version": "1.0.0",
  "description": "CSV Manager Backend API",
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/app.js",
    "start": "node src/app.js",
    "build": "npm install && npx prisma generate && npx prisma db push --schema prisma --force-reset",
    "postinstall": "npx prisma generate",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "@tailwindcss/vite": "^4.1.8",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "papaparse": "^5.4.1",
    "prisma": "^5.7.0",
    "tailwindcss": "^4.1.8"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
